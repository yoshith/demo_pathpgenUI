{
  "name": "battery-flow",
  "version": "0.1.0",
  "private": true,
  "description": "NYC Battery microbial transport research map with NOAA observations and clearly labeled simulation.",
  "type": "module",
  "engines": { "node": ">=22 <25" },
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "build": "node scripts/check.mjs",
    "test": "node --test test/*.test.mjs",
    "check": "npm run build && npm test"
  }
}
